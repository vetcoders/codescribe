# CodeScribe v0.14.1 release readiness — 2026-08-22

Status: **LOCAL DISTRIBUTION CANDIDATE ACCEPTED; PUBLICATION NOT YET
AUTHORIZED.** Artifact commit
`3deadbdf852c8994edfee9d7ce1fc7a293012509` is pushed, and its exact signed DMG
passed notarization, stapling, Gatekeeper, checksum, and repository payload
verification. Installed-app smoke and the tag/appcast/GitHub Release remain
separate operator-controlled gates.

This record resolves the two-part deep review of PR #82 against the release
candidate actually selected by the operator. It is not a claim that PR #82's
head was merged wholesale.

## Candidate and branch topology

- Selected working line: `dbxms-runtime-claude`, tracking
  `origin/fix/dbxms-by-claude`.
- Baseline at the start of the release cut: `8b2f7fdc`.
- Competing review head: `origin/fix/p0-p2-before-release@1f9cf68d`.
- The two lines diverged after `a95e1272` and overlapped in seven runtime
  owners. They were not mechanically merged.
- `0a13713f` contributed the acoustic-identity contract and parked
  falsifiers. The competing Grok acoustic ledger implementation is explicitly
  **NOT MERGED** into this candidate; its `admit_one` findings therefore remain
  branch-specific rather than hidden release debt.

Runtime and documentation fixes were ported independently only where the
candidate had the same product contract and an auditable owner.

## Review part 1 disposition

| ID    | Disposition on this candidate      | Evidence or remaining action                                                                                                                                                  |
| ----- | ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P0-01 | Fixed                              | `h2` is updated to 0.4.16 in `Cargo.lock`; `cargo audit` reports no vulnerability and 12 explicitly allowed maintenance/unsoundness warnings.                                 |
| P1-01 | Fixed                              | Tail-patch receipts keep independently counted abandoned work instead of manufacturing the reconciliation remainder.                                                          |
| P1-02 | Not applicable to selected runtime | The hard-coded Apple observation identity belongs to the competing acoustic ledger that was not merged.                                                                       |
| P1-03 | Fixed                              | Failed worker startup rolls the application runtime back; retry cannot report a degraded runtime as healthy.                                                                  |
| P1-04 | Fixed                              | Controller shutdown has a bounded wait before the global runtime shutdown.                                                                                                    |
| P1-05 | Fixed by this release cut          | Private source journal is distilled into canonical contracts/readiness and removed; public fixtures are role-based.                                                           |
| P2-01 | Verified on candidate tree         | Local `make check`, `make verify`, and `make test-swift` pass; no green result from a different remote head is used as evidence.                                              |
| P2-02 | Not applicable to selected runtime | Left-overlap admission belongs to the unmerged competing acoustic ledger.                                                                                                     |
| P2-03 | Not applicable to selected runtime | Multi-prior admission belongs to the unmerged competing acoustic ledger.                                                                                                      |
| P2-04 | Not applicable to selected runtime | Multi-session ledger projection/sorting belongs to the unmerged competing acoustic ledger.                                                                                    |
| P2-05 | Fixed                              | Active names canonicalize the complete tail (`IWO` → `Iwo`).                                                                                                                  |
| P2-06 | Fixed                              | Compiled active-name regexes are cached until the bounded name set changes.                                                                                                   |
| P2-07 | Fixed                              | Bus follower uses the canonical schema/path resolution, commits its cursor after successful delivery, and does not replay from zero after shrink.                             |
| P2-08 | Fixed                              | Layer 1 documentation now distinguishes the raw env default from the Local Power mode-owned default. Dead `AGENT_BUS.md` was removed.                                         |
| P2-09 | Fixed                              | Paste success requires exact observation of the foreign target; the overlay remaining frontmost is a recoverable refusal.                                                     |
| P2-10 | Fixed                              | Installer verification failures remain fail-closed and now emit a privacy-safe unified-log event without local paths.                                                         |
| P2-11 | Open, non-blocking P2              | Manifest hashing is synchronous. Cache by bundle version/hash if the payload grows enough to affect Settings responsiveness.                                                  |
| P2-12 | Partial                            | Refusal behavior is typed and tested at the boundary; retain two explicit branch falsifiers as follow-up coverage.                                                            |
| P2-13 | Fixed                              | The acoustic-final rewrite veto is scoped to `LayerSource::TailPatch`.                                                                                                        |
| P2-14 | Fixed                              | The span-idempotence default flip is explicit in the changelog.                                                                                                               |
| P2-15 | Open verification                  | Swift task cancellation still requires a host/manual propagation probe; comments must not imply automatic cross-FFI cancellation.                                             |
| P2-16 | Fixed                              | Silero sideband retention is bounded; the upstream VAD boundary queue is also capped/drained.                                                                                 |
| P2-17 | Superseded                         | CI did run on the reviewed PR head; it was red for P0-02. This candidate relies on its own final gates.                                                                       |
| P3-01 | Deferred test hygiene              | Remaining Rust 2024 test-only environment mutations need uniform `SAFETY` comments; the tested runtime contract and release gates are unaffected.                             |
| P3-02 | Deferred code cleanup              | The legacy continuous VAD session still hard-disables its shadow tail-audio branch; live Apple Layer 1 uses the canonical window producer. Remove the dead branch explicitly. |
| P3-03 | Accepted temporary debt            | Receipt warning codes are locked by tests, but the compatibility `from_events` adapter still parses typed state from event text. Replace it with typed transport.             |
| P3-04 | Fixed / stale                      | The reviewed `usize::MAX / 4` sentinel no longer exists on this candidate.                                                                                                    |
| P3-05 | Fixed                              | The reviewed hotkey `eprintln!` path is gone; current error reporting uses structured logging.                                                                                |
| P3-06 | Mitigated; stress probe open       | The foreign-target cache is typed, expiring, process-validated, and mutex-serialized. A concurrent-hotkey stress test remains worthwhile.                                     |
| P3-07 | Accepted product contract          | Missing energy fails aggregate word-span coverage closed while retaining sealed text; partial false anchoring is intentionally not published.                                 |
| P3-08 | Fixed                              | Demux and live/seal documentation now use the canonical bus schema and `CODESCRIBE_DATA_DIR`-aware resolver.                                                                  |
| P3-09 | Deferred diagnostics               | The DMG payload verifier still needs to capture Python stderr in its failure receipt; successful verification remains fail-closed.                                            |
| P3-10 | Deferred API cleanup               | `shouldShowOverlay` intentionally follows `trayEnabled`, but its ignored compatibility parameters should be removed in the next Swift API cleanup.                            |
| P3-11 | Partial cleanup                    | The mean-energy identity tripwire is gone. The unconstructed `LocalTailPatchDisposition::NotApplicable` compatibility variant remains for a later type cleanup.               |
| P3-12 | Deferred lexicon hardening         | Human provenance and N=3 promotion are enforced. Unknown provenance logging, a configured-threshold cap, and progress-receipt serialization remain follow-ups.                |
| P3-13 | Fixed by deprivatization           | Public documentation uses role names instead of founder names.                                                                                                                |
| P3-14 | Accepted pre-existing debt         | `cargo audit` has no vulnerability; 12 configured GTK3-era maintenance/unsoundness advisories remain and need a dependency-exit plan.                                         |
| P3-15 | Deferred onboarding UX             | Agent Bridge install errors are visible, but the synchronous button still lacks progress state and fatal startup diagnostics remain system-log-only.                          |

## Review part 2 disposition

| ID    | Disposition on this candidate | Evidence or remaining action                                                                                  |
| ----- | ----------------------------- | ------------------------------------------------------------------------------------------------------------- |
| P0-02 | Fixed                         | Aborted runtime tasks reconcile ownership; cancellation/shutdown tests exercise the real counter.             |
| P1-06 | Fixed                         | Every programmatic formatted-text write clears human-edit provenance; tests cover late machine writes.        |
| P1-07 | Fixed                         | Demux persists the lease cursor only after successful emit and has broken-pipe coverage.                      |
| P1-08 | Fixed                         | Any failed aggregate coverage clears every published word span.                                               |
| P1-09 | Fixed                         | Empty or foreign inline-ledger state falls through to one-shot formatting; a test uses the real global store. |
| P1-10 | Fixed                         | Capture ownership is released by terminal dispatch, not solely by a potentially lagged broadcast event.       |
| P1-11 | Fixed                         | Agent-bridge staging refuses repository roots, ancestors, and symlinked sources before replacement.           |
| P1-12 | Fixed                         | Assistive capture latches the target thread at capture start.                                                 |
| P2-18 | Fixed                         | Demux accepts only `codescribe.transcript.v1`.                                                                |
| P2-19 | Fixed                         | Reserved layered phases are degraded/invalid rather than silently armed.                                      |
| P2-20 | Fixed                         | The CodeScribe skill installs/owns one follower instead of attempting a second live lease.                    |
| P2-21 | Fixed                         | Bridge packaging rejects source and staged symlinks.                                                          |
| P2-22 | Fixed                         | Apple-only Settings copy no longer claims live Whisper refinement.                                            |
| P2-23 | Fixed by contract             | The Agent window is not advertised as a Cmd+V ambulance; Agent delivery uses its explicit route.              |
| P2-24 | Fixed                         | Cached foreign targets expire, must still be running, and are never relaunched by AppleScript.                |
| P2-25 | Fixed                         | Swift installer honors `CODESCRIBE_AGENT_BRIDGE_HOME`.                                                        |
| P2-26 | Fixed                         | Bus shrink advances to the current end instead of replaying private history.                                  |
| P2-27 | Fixed by explicit product law | Dictionary Teach is a visible bulk operator override; automatic learning still requires provenance and N=3.   |
| P2-28 | Fixed                         | Layer 1 emits one request per contiguous PCM region instead of inventing zero-filled exact audio.             |
| P2-29 | Fixed                         | VAD boundaries are capped/drained on the VAD-only path.                                                       |
| P2-30 | Fixed                         | Cross-utterance `ReplaceRange` is refused rather than poured into the first fragment.                         |
| P2-31 | Deferred, default OFF         | Silero fusion remains diagnostic-only until enclosing-range semantics and live A/B evidence agree.            |
| P2-32 | Fixed                         | Recording Down/Up events use one FIFO Tokio consumer.                                                         |
| P2-33 | Fixed                         | Inline L3 fails closed unless the selected provider uses the Responses-family wire.                           |
| P3-16 | External PR maintenance       | PR #82's body describes the competing head. Do not rewrite it as evidence for this candidate.                 |
| P3-17 | Fixed                         | Installer validates and applies manifest modes.                                                               |
| P3-18 | Fixed                         | Both Layer 1 warning codes have event-level assertions.                                                       |
| P3-19 | Fixed                         | Bus logs omit absolute home paths and the schema contract includes runtime fields.                            |
| P3-20 | Open, non-blocking P3         | Hotkey TCC startup failure needs a user-visible recovery notice in addition to system logging.                |
| P3-21 | Fixed                         | L3 tests exercise process-global store isolation.                                                             |
| P3-22 | Fixed                         | Bridge quality prose matches the executable human-provenance/N=3 contract.                                    |

## Public-release hygiene

- The public tree must not contain workstation paths, personal emails, private
  transcript fixtures, session exports, API keys, tokens, notarization
  credentials, or identity-bearing artifact bundles.
- The OAuth UUID in the account-auth module and `NOTICE` is a public product
  client identifier used by live authentication code. It remains a **REVIEW
  P0** in the external naming ledger; do not scrub it as if it were a leaked
  runtime session token without replacing the functional OAuth registration.
- Developer signing, notarization, Voice Lab, and credential source paths are
  operational integrations. Public docs describe their contract, not operator
  machines or people.
- The two private `w2_05_*` fixtures are excluded by an operator-owned
  `.gitignore` change and must never be staged into this release.

## Release gate

### Evidence collected on the candidate tree

- `make check`: PASS; Clippy denied warnings, Semgrep scanned 621 tracked
  files with 0 findings, and the env/gate registries passed.
- `make verify`: PASS; workspace tests, hermetic e2e suites, bridge tests,
  doctests, model-promotion regression, and registry checks completed.
- `make test-swift`: PASS; phrase-restart lockstep passed and 406 Swift tests
  completed with 0 failures and 1 declared skip.
- `cargo audit`: PASS with no vulnerability; 12 configured warnings remain for
  unmaintained GTK3-era dependencies and `glib` unsoundness.
- Deprivatization verify: PASS with no unambiguous PII/secret leak. The final
  critical report contains three synchronized occurrences of the functional
  public OAuth client identifier, classified `needs_decision` and recorded as
  REVIEW P0 in the external naming ledger. History scan flagged 15 commits for
  report-only review; it does not rewrite Git history.

The complete source gates were collected on the candidate implementation tree.
The artifact commit then changed only the release journal and regenerated
UniFFI Swift binding/checksum; the Swift suite was rerun after regeneration.
That exact commit produced and passed the distribution artifact below.

### Final artifact closure

- Artifact commit: `3deadbdf852c8994edfee9d7ce1fc7a293012509`, branch synchronized
  `0/0` with `origin/fix/dbxms-by-claude` when packaged.
- Artifact: `Codescribe_0.14.1-20260822-3deadbdf8.dmg` (slim standard SKU).
- Apple notarization: **Accepted** for the exact DMG; the account-scoped
  submission receipt remains in the local operator handoff, not the public tree.
- Ticket: stapled to both `.app` and DMG; Gatekeeper accepted the notarized
  Developer ID application.
- Post-staple SHA-256:
  `5c6ee05d491822464ffaccf8337446ccbfcc1f91b980a8ba16268a007c83b113`;
  the sidecar matches the DMG bytes.
- `verify-dmg`: PASS — app `0.14.1`, deep/strict signature, eight Agent Bridge
  payload files, signed MiniLM runtime resource, slim dylib, and declared
  standard payload all accepted.

The source and local distribution rows are therefore complete:

1. [x] `cargo fmt --all -- --check` and `git diff --check`.
2. [x] `make check` (formatting, Clippy, Semgrep, env registry, ledgers).
3. [x] `make verify` (workspace tests and doctests).
4. [x] `make test-swift` after regenerated bindings/project state.
5. [x] `cargo audit` with no blocking advisory.
6. [x] Final deprivatization and tracked-history review with no unreviewed P0
       secret/PII leak; the functional public OAuth registration remains a named
       external-review decision.
7. [x] Exact-commit Developer ID build, package, notarization, staple, and
       `verify-dmg` acceptance.

Installation was deliberately not performed while the Transcript Bus reported
an open take. The DMG was opened for the operator; installing that exact app
and running the host microphone/delivery smoke remains the final runtime gate.

No tag, trunk merge, GitHub Release, or public announcement is authorized by
this readiness record.

## Next steps after v0.14.1

1. Add the two remaining `TAIL_PATCH_APPLY_REFUSED` branch falsifiers.
2. If measured UI latency warrants it, add a version-keyed verification cache
   to `AgentBridgeInstaller`.
3. Run and document the Swift-to-Rust cancellation probe.
4. Add an operator-visible hotkey/TCC recovery notice.
5. Keep Silero fusion OFF until a live corpus proves its range binding and
   pause evidence improve outcomes without widening rewrite authority.
6. Revisit the compact overlay control surface separately; it is not part of
   the transcript-authority release cut.
7. Close the explicit part-one P3 cleanup ledger: add uniform test `SAFETY`
   comments, remove the legacy disabled tail branch and ignored overlay API
   parameters, type the receipt path end-to-end, and prune the unused
   `NotApplicable` variant if no caller appears.
8. Add a concurrent-hotkey cache stress test, preserve fail-closed aggregate
   word-span coverage, and capture Python stderr in failed DMG payload receipts.
9. Harden lexicon diagnostics and bounds, publish a GTK3 dependency-exit plan,
   and add Agent Bridge install progress plus a durable fatal-startup receipt.
