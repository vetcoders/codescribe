---
run_id: local-vc-release-20260623-codex
agent: codex
skill: vc-release
project: Codescribe
status: blocked
created: 2026-06-23T18:59:30Z
release_version: v0.12.2
---

# Release Report: Codescribe v0.12.2

Verdict: **blocked**. The repository is public after hydration, but the release is not safe to announce because no current published `v0.12.2` artifact exists, the hardened release workflow has not landed on `main`, GitHub Actions signing/notary secrets are not configured, GitHub license detection still needs review, and the local DMG is rejected by macOS Gatekeeper assessment.

## 1. Security gate

- Command run: `make semgrep`
- Exit status: `0`
- Finding count: `0` blocking findings observed from the quiet release security scan.
- Gate satisfied: `yes`

| Rule ID | Severity | File | Lines | Boundary | Resolution                                         |
| ------- | -------- | ---- | ----- | -------- | -------------------------------------------------- |
| N/A     | N/A      | N/A  | N/A   | N/A      | `make semgrep` exited 0 with no reported findings. |

Additional full gate evidence from the same hydration run:

- `make check` exited 0.
- Clippy finished for `codescribe-core v0.12.2` and `codescribe v0.12.2`.
- Semgrep scanned 343 tracked files and reported `0 findings`.
- Existing Prettier warnings remain in historical docs outside the touched public-release patch; targeted Prettier checks for touched public files passed.

## 2. Exposed surface inventory

| Surface              | Bind address          | Port | Public?                        | Proxy in front      | TLS terminator | Auth boundary                                              |
| -------------------- | --------------------- | ---- | ------------------------------ | ------------------- | -------------- | ---------------------------------------------------------- |
| Codescribe macOS app | local desktop process | N/A  | no                             | none                | none           | macOS user session, TCC permissions, Keychain for API keys |
| GitHub Pages landing | GitHub Pages managed  | 443  | yes                            | GitHub Pages/Fastly | GitHub Pages   | public                                                     |
| GitHub repository    | GitHub.com            | 443  | yes                            | GitHub              | GitHub         | public repository                                          |
| GitHub Releases DMG  | GitHub.com            | 443  | partially: stale `v0.8.0` only | GitHub              | GitHub         | public release asset when published                        |

Edge headers observed for `https://vetcoders.github.io/codescribe/` on 2026-06-23:

- `HTTP/2 200`
- `strict-transport-security: max-age=31556952`
- `content-type: text/html; charset=utf-8`
- `last-modified: Thu, 07 May 2026 03:13:49 GMT`

Site indexability evidence:

- `robots.txt` allows all and points to `https://vetcoders.github.io/codescribe/sitemap.xml`.
- `sitemap.xml` lists `/Codescribe/` and `/Codescribe/quickstart/`.
- Current live Pages content is stale relative to this branch until the landing changes reach the Pages deployment branch.

Secret materialization:

- App runtime API keys are configured via macOS Keychain or `~/.codescribe/.env`.
- Release workflow expects GitHub Actions secrets:
  `CODESIGN_CERTIFICATE_BASE64`, `CODESIGN_CERTIFICATE_PASSWORD`, `CODESCRIBE_CODESIGN_IDENTITY`, `APPLE_ID`, `APPLE_TEAM_ID`, `APPLE_APP_SPECIFIC_PASSWORD`.
- Release workflow now fails fast when those secrets are missing.
- `gh secret list --repo vetcoders/Codescribe` and `gh variable list --repo vetcoders/Codescribe` returned no entries on 2026-06-23. That means the hardened workflow would currently fail before building, which is safer than publishing an ad-hoc public DMG.

Forbidden patterns checked:

- No public app service bind is expected; Codescribe is a desktop app.
- Release DMG publication is blocked until Developer ID signing and notarization are configured.
- No `.env` files are tracked; `.env.example` and `.env.debug.example` remain allowed examples.

## 3. Deployment mode decision

- Chosen topology: static landing via GitHub Pages plus GitHub Releases for notarized DMG distribution.
- Reason this is the smallest honest fit: Codescribe is a local macOS desktop app, so it does not need an app server. The public surface needs a crawlable landing page, quickstart, release notes, and a trustworthy signed DMG artifact.

Topology sketch:

```text
stranger -> GitHub Pages landing -> quickstart / GitHub Release
                                  -> Codescribe_0.12.2.dmg
                                  -> local macOS install -> TCC permissions -> tray app
```

Healthcheck path and expected response:

- Landing: `curl -I -L https://vetcoders.github.io/codescribe/` should return `HTTP/2 200`.
- Robots: `https://vetcoders.github.io/codescribe/robots.txt` should allow crawl.
- Sitemap: `https://vetcoders.github.io/codescribe/sitemap.xml` should list landing and quickstart.

Restart and graceful shutdown behavior:

- Desktop runtime uses macOS app lifecycle; no public daemon deployment is part of the launch topology.
- `qube-daemon` remains local tooling, not a public network service.

Rollback procedure:

- Landing rollback: revert the `docs/landing/**` commit on the Pages deployment branch and let `.github/workflows/pages.yml` redeploy.
- Release rollback: delete or mark the GitHub Release as pre-release/broken, remove the bad DMG asset, and publish a patch tag only after a notarized artifact passes cold install smoke.

## 4. Post-release install smoke

Status: **blocked**.

Artifact source:

- Published URL: missing. `gh release list --limit 5` reports latest release `v0.8.0` from `2026-03-09T08:07:13Z`.
- Tags: `git ls-remote --tags origin` lists `v0.1.0`, `v0.6.0`, and `v0.8.0`; no `v0.12.2` tag exists remotely.
- Local-only artifact observed: `Codescribe_0.12.2.dmg`
- Local SHA256: `49fa9186a01dac48b2a51715cec47f202d394a0c481e47ba350a78feb35dba2c`
- Local artifact status: not release evidence because it is a working-copy file, not a published GitHub Release asset.

Cold environment used:

- None. A published artifact does not exist.

Command sequence executed:

```bash
gh release list --limit 5
git ls-remote --tags origin | tail -20
shasum -a 256 Codescribe_0.12.2.dmg
hdiutil imageinfo Codescribe_0.12.2.dmg
spctl -a -vv -t open Codescribe_0.12.2.dmg
curl -I -L --max-time 15 https://vetcoders.github.io/codescribe/
curl -s --max-time 15 https://vetcoders.github.io/codescribe/robots.txt
curl -s --max-time 15 https://vetcoders.github.io/codescribe/sitemap.xml
```

First-run evidence:

- Local DMG image format: `UDIF read-only compressed (zlib)`, `Format: UDZO`, size approximately `1.35 GB` encoded.
- Gatekeeper assessment: `Codescribe_0.12.2.dmg: rejected`, `source=Insufficient Context`.
- No launch/version evidence from a published artifact exists.

Drift observed against the documented quickstart:

- Live Pages still serves older content with `last-modified: Thu, 07 May 2026 03:13:49 GMT`; this branch's quickstart changes are not live yet.
- `origin/main` still has the old release workflow shape that runs `make dmg`; the signed/notarized `make release-full` workflow is present in this branch and must land before creating `v0.12.2`.
- Public GitHub repo metadata was corrected on 2026-06-23: visibility `PUBLIC`, homepage `https://vetcoders.github.io/codescribe/`, and launch topics are present. GitHub license display still reports Apache-2.0 and needs review against the active `FSL-1.1-ALv2` license text.

## Sign-off

- Security gate: ok
- Surface inventory: blocked until license display, Pages freshness, and release workflow sequencing are corrected
- Deployment mode: ok for static landing + GitHub Release distribution
- Install smoke: blocked until signing/notary secrets are configured, `v0.12.2` is tagged from the hardened workflow, published, signed, notarized, downloaded from GitHub Releases, and launched cold

Released by: not released. Report generated by `codex` on `2026-06-23T18:59:30Z`.
