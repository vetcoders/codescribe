# VistaScribe runtime dependencies (Python >=3.12, macOS arm64)
# This list is intentionally lean and reflects actual runtime imports in the app.
# Tools that prefer requirements.txt over pyproject.toml can use this file.

# --- Core tray app (menu bar UI + hotkeys + HTTP client) ---
rumps==0.4.0
pyobjc-framework-Quartz
requests>=2.32.3
python-dotenv>=1.0.0
numpy>=2.0.0
sounddevice>=0.4.6

# --- Local ML backends (lazy-loaded; install to enable local STT/LLM) ---
mlx-whisper>=0.4.3
mlx-lm>=0.28.1

# --- Backend API server ---
fastapi>=0.115.0
pydantic>=2.0.0
uvicorn>=0.30.0

# --- Optional extras (uncomment if needed) ---
# openai>=1.0.0        # enable OpenAI formatter (FORMAT_STRATEGY=openai)
# mlx-audio>=0.2.5     # optional TTS server/client helpers
# mlx-embeddings>=0.0.4

# --- Build tools (optional for .app/.dmg packaging) ---
# py2app>=0.28.8
