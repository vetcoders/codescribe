# CodeScribe Configuration
# Generated automatically - edit with care

# === Secrets ===
STT_API_KEY=vista-
# LLM_API_KEY=sk-proj-
LLM_ASSISTIVE_API_KEY=
LLM_FORMATTING_API_KEY=

# === Core mode ===
USE_LOCAL_STT=1
LOCAL_MODEL=whisper-large-v3-turbo-mlx-q8
WHISPER_LANGUAGE=pl

# === Hotkeys ===
HOLD_MODS=ctrl
HOLD_EXCLUSIVE=1
TOGGLE_TRIGGER=none
HOLD_START_DELAY_MS=800

# === Overlay / UI ===
OVERLAY_POSITION_MODE=snapped_top_right
SHOW_TRAY_GLYPH=1
HOLD_INDICATOR=1
HOLD_BADGE_SIZE=8
HOLD_BADGE_OFFSET_X=10
HOLD_BADGE_OFFSET_Y=-10

# Two reasonable configurations (choose 1)

## ✅ B) "Live preview" (recommended default)
# Shows live deltas in the small overlay while holding Ctrl.
CODESCRIBE_BUFFERED_STREAM=0
CODESCRIBE_STREAM_CHUNK_SEC=2.0
CODESCRIBE_STREAM_OVERLAP_RATIO=0.25
# (optional) keep these for experimentation / smoother feel:
# CODESCRIBE_BUFFER_DELAY_MS=1800
# CODESCRIBE_TYPING_CPS=35
# CODESCRIBE_EMIT_WORDS_MAX=3
# VAD config (centralized in core/vad/config.rs)
# Simple controls (preferred):
##### CODESCRIBE_VAD_SENSITIVITY=0.5 #####
##### CODESCRIBE_VAD_SILENCE_SEC=1.2 #####
# Advanced controls (override when needed):
# CODESCRIBE_VAD_THRESHOLD=0.5
# CODESCRIBE_VAD_MIN_SPEECH_SEC=0.1
# CODESCRIBE_VAD_MAX_SILENCE_SEC=1.2
# CODESCRIBE_VAD_MAX_UTTERANCE_SEC=60
# CODESCRIBE_VAD_PRE_ROLL_SEC=0.3
CODESCRIBE_STREAM_SIMILARITY=0.90
CODESCRIBE_STREAM_NOVELTY=0.20
# Embedder model override (HuggingFace cache repo id)
# CODESCRIBE_EMBEDDER_REPO=intfloat/multilingual-e5-small

# ---------------------------------------------------------------------------

## ✅ A) "Hands-off stable" (buffered, less jitter)
# Emits at end-of-utterance with a small delay; can feel more stable but is not "live".
# CODESCRIBE_BUFFERED_STREAM=1
# CODESCRIBE_STREAM_CHUNK_SEC=3.0
# CODESCRIBE_STREAM_OVERLAP_RATIO=0.2
# CODESCRIBE_BUFFER_DELAY_MS=1800
# CODESCRIBE_TYPING_CPS=35
# CODESCRIBE_EMIT_WORDS_MAX=3

# === LLM / Assistive & Formatting ===
AI_FORMATTING_ENABLED=1
TRANSCRIPT_SEND_MODE=streaming
ASSISTIVE_CONTEXT_MAX_CHARS=20000
#
# PDF attachments: OCR language hint (used when OCR is needed; prefers offline tools)
CODESCRIBE_ATTACH_PDF_OCR_LANG=pol+eng
LLM_ASSISTIVE_ENDPOINT=https://api.openai.com/v1/responses
LLM_ASSISTIVE_MODEL=gpt-5.2
# LLM_ASSISTIVE_TEMPERATURE=0.2
LLM_FORMATTING_ENDPOINT=http://dragon:10240/v1/responses
LLM_FORMATTING_MODEL=libraxisai/Svetliq-11B-v3.0-mlx-7000-v1-preview
LLM_FORMATTING_TEMPERATURE=0.1
LLM_USE_STREAMING=1

# === Cloud STT ===
STT_ENDPOINT=https://api.libraxis.cloud/v1/audio/transcriptions

# === History / logs ===
HISTORY_ENABLED=1
DUMP_AUDIO_LOGS=1

# === Quick Notes (voice → text notes) ===
# When enabled, dictation saves to a daily Markdown note.
QUICK_NOTES_ENABLED=0
# If enabled, Quick Notes becomes "save-only" (no auto-paste).
QUICK_NOTES_SAVE_ONLY=0
# Optional override for notes directory (Vista-friendly):
# CODESCRIBE_NOTES_DIR=~/Git/vista/notes

# === Clipboard ===
RESTORE_CLIPBOARD=1
RESTORE_CLIPBOARD_DELAY_MS=1000

# === Audio ===
AUTO_SILENCE=0
BEEP_ON_START=1
SOUND_NAME=Tink
SOUND_VOLUME=0.25

# === Debug (optional) ===
# CODESCRIBE_STREAM_LOG=1
# CODESCRIBE_STREAM_LOG_PATH=/tmp/codescribe_stream.log
# RUST_LOG=full
# LOG_LEVEL=error
