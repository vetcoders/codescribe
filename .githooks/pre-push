#!/usr/bin/env bash
set -euo pipefail

echo "[pre-push] Ruff check…" >&2
if command -v uvx >/dev/null 2>&1; then
  RUFFF=(uvx ruff)
else
  RUFFF=(ruff)
fi

"${RUFFF[@]}" check .
echo "[pre-push] OK" >&2

# tip: bypass with `git push --no-verify` if needed
